# Loaded automatically by `vite build`. This is the deployed backend, not a
# secret — the browser calls it directly. A VITE_API_BASE_URL set in the
# hosting environment (e.g. an App Platform BUILD_TIME variable) wins over it.
VITE_API_BASE_URL=https://monkfish-app-6ss4k.ondigitalocean.app
